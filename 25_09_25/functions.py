def add_numbers(a, b): # Simple function to add two numbers
    return a + b

def increment(x): # Function to increment a number by 1
    return x + 1

def increment_by(x, amount): # Function to increment a number by a specified amount
    return x + amount