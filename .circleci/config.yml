version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.12
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            python -m venv venv
            . venv/bin/activate
            pip install pandas matplotlib pytest
      - run:
    name: Run tests (only 23_10_25)
    command: |
      source venv/bin/activate
      pytest 23_10_25/ -v